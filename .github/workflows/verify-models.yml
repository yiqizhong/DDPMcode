name: Verify headset models

on:
  push:
  pull_request:

jobs:
  verify-models:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Verify generated headset models
        run: bash .agents/skills/headset-gen-subpage/verify-all.sh
